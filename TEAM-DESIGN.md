# Team Index Page (PAGE_11) — Implementation Plan

**Source of truth:** `/home/lordwhitefire/Downloads/whitefire-team-page-11.md` (UI spec).
**Status:** Plan — waiting for "go" before coding.

---

## 1. What this page is

The Whitefire Interior **Team** page at `/team`:

- dark cinematic hero (eyebrow `OUR TEAM` + serif `The People Behind Timeless Spaces` + supporting copy);
- centered intro (eyebrow `MEET THE TEAM` + `Expertise. Creativity. Collaboration.`);
- five-column team grid — **5 real staff members** (portrait, name, role, bio, LinkedIn);
- values section (eyebrow `OUR VALUES`, 4 columns, icon + title + description, thin dividers);
- split project CTA (eyebrow `LET'S WORK TOGETHER`, `Have a Project in Mind?`, bronze `SCHEDULE A CONSULTATION` button → `/contact`);
- shared `SiteHeader` (TEAM active) + **original `SiteFooter` (unchanged)**.

**Data:** REAL Sanity staff (snapshot → JSON). Page shows **5 members (orders 1–5)**. The other 3 staff (Javier, Clara, Noah) remain in Sanity untouched — not shown.

---

## 2. Decisions already made (owner-confirmed)

| Question | Decision |
|---|---|
| Members shown | **5**, per the md spec — the first 5 real staff by order: Elena Vasquez, Marcus Chen, Sofia Rahman, Liam O'Connor, Aisha Patel |
| Fictional members (Sophie van Dijk etc.) | Rejected — never use fake people |
| Other 3 staff (Javier Morales, Clara Dubois, Noah Kim) | **Leave their data untouched** (still in Sanity); just not rendered |
| Routes | `app/routes/team.tsx` becomes a bare `<Outlet/>` layout (replaces the 101-line legacy page, prevents the blog-style nesting bug); new `app/routes/team._index.tsx` holds the page |
| Legacy routes | `teams.tsx` / `teams.$slug.tsx` left untouched |
| Footer | **Original `SiteFooter` (tiny dark placeholder) — NOT the package's 5-column ivory footer** (site-wide owner rule) |
| Header | Shared `SiteHeader` with `activePath="/team"` (TEAM active) — one header for all team pages |
| Stage | Same framed 1440px pattern as services/projects/blog |
| Legacy chrome | Add `/team` + `/team/*` to `hideGlobalHeader` in `root.tsx` |
| Data boundary | `app/data/team/staff.json` (generated by snapshot script from Sanity — real names/roles/bios/photos/social/order); static page content (hero, intro, values, CTA) as `app/data/teamMock.ts` const |
| Roles | **Real roles from Sanity** (e.g. Aisha = "Sustainability Director"), not package mock roles |
| Values | 4 static values from the package (Client-Centered / Thoughtful Design / Quality & Integrity / Sustainable Approach); Lucide icons `UsersRound` `PencilRuler` `ShieldCheck` `Leaf`, stroke ~1.25, bronze |
| CTA | Package copy verbatim; button → `/contact` |
| Portraits | Staff photos are 1600×896 landscape → served at 4:5 crop (`object-cover`); faces verified via screenshots, adjust crop after review |
| Team hero image | From the unused project-gallery pool (interior photo, alt from source project title) |
| Meta | `Our Team | Whitefire Interior` + package description |

---

## 3. Page structure (top to bottom)

```
<div framed stage (E8E2D8 outer + 1440px panel F7F4EE)>
  SiteHeader (activePath="/team")
  <main>
    TeamHero          — min-h 397/500px, full-bleed image, left dark gradient, OUR TEAM eyebrow,
                        serif title 42→62px, description, content bottom-left
    TeamIntroduction  — centered: MEET THE TEAM eyebrow, serif h2, bronze hairline, 2-line description
    TeamGrid          — max-w 1320px, grid-cols-1 / sm:2 / lg:5, gap 14–16px
      TeamCard × 5    — border #ddd9d2, portrait aspect-[0.84] (hover scale 1.025, 700ms),
                        name serif 19px, role 7px bronze uppercase, bio 10px, LinkedIn icon
    ValuesSection     — bg #EEE8E8 divider band; 4 columns separated by thin vertical rules (lg),
                        icon 31px bronze, title, description
    ProjectCTA        — grid 58/42 (image left / dark panel right), eyebrow, serif h2,
                        description, bronze button + arrow
  </main>
  SiteFooter (original)
```

---

## 4. Data files

### `app/data/team/staff.json` (generated — real Sanity data)

New snapshot script `scripts/snapshot-sanity-staff.cjs` (pattern: `snapshot-sanity-projects.cjs`):

```json
{
  "elena-vasquez": { "slug", "fullName", "role", "order", "featured",
                     "bio": "<text>", "photoUrl": "<cdn + w=600&h=750&fit=crop>",
                     "social": [{ "platform", "url" }], "metaTitle", "metaDescription" },
  ... 8 members (orders 1–8)
}
```

- bio: portable-text blocks flattened to plain text (first block text).
- photoUrl: `w=600&h=750&fit=crop&crop=center` (4:5 portrait).
- Page reads members with `order <= 5`; orders 6–8 remain in the file (harmless, not rendered).

### `app/data/teamMock.ts` (static spec content)

- `hero` — OUR TEAM / The People Behind Timeless Spaces / description / image (pool)
- `intro` — MEET THE TEAM / Expertise. Creativity. Collaboration. / description
- `values` — 4 items (icon key + title + description)
- `projectCta` — LET'S WORK TOGETHER / Have a Project in Mind? / description / button / `/contact`

---

## 5. Images / pool accounting

- Portraits: 5 real staff photos (identity — new usage, used once).
- Team hero (1) + CTA (1): from unused project-gallery pool.
- Unused pool after blog: **97** → team uses **2** → **95 remain** for the detail pages (heroes + featured cards + CTA ≈ 33).

---

## 6. Interaction / a11y / SEO

- Card hover: portrait scale 1.025 + warm border, 500–700ms; no lift/shadow.
- Values icons `aria-hidden`; LinkedIn link `aria-label="<name> on LinkedIn"`.
- Heading order: h1 hero → h2 intro → h3 member names → h3 value titles.
- Images: hero `fetchPriority="high"` eager; portraits lazy.
- Meta: `Our Team | Whitefire Interior` + package description.

---

## 7. Verification & acceptance

- `npm run build` passes.
- `/team` → 200: OUR TEAM hero, 5 real member cards (names/roles/bios/LinkedIn), values ×4, CTA, no legacy chrome (`role="banner"/contentinfo` absent).
- Screenshots: desktop 1920 + mobile 390 full-page → `/home/lordwhitefire/interior-deoc-screenshot/team-*.png`.
- Then wait for owner's review before the detail page work continues (detail doc follows separately).